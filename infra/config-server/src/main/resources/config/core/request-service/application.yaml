server:
  port: 0

spring:
  application:
    name: request-service
  cloud:
    openfeign:
      okhttp:
        enabled: true
  dataSource:
    driverClassName: org.postgresql.Driver
    password: sa
    url: jdbc:postgresql://localhost:6553/request-service
    username: sa
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always

resilience4j.circuitbreaker:
  configs:
    default:
      slidingWindowSize: 10
      failureRateThreshold: 50

grpc:
  client:
    collector:
      address: 'discovery:///collector'
      enableKeepAlive: true
      keepAliveWithoutCalls: true
      negotiationType: plaintext

hibernate:
  jdbc:
    time_zone: UTC
  show_sql: true
